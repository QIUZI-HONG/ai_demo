# AI 提供方配置（密钥仅存服务端，切勿暴露到前端或提交仓库）
# 任选一个 OpenAI 兼容服务，例如 OpenAI / DeepSeek / 通义千问 等
AI_API_KEY=sk-your-key-here
AI_BASE_URL=https://api.openai.com/v1
AI_MODEL=gpt-3.5-turbo

# 不填 AI_API_KEY 时，应用自动进入「离线兜底模式」，仍可演示流式与 function calling 逻辑
